{
  "name": "fuelsphere-airports",
  "version": "1.0.0",
  "description": "FuelSphere - Airports Master Data",
  "scripts": {
    "build": "ui5 build preload --clean-dest --config ui5-deploy.yaml --include-task=generateManifestBundle generateCachebusterInfo && cp xs-app.json dist/ && cd dist && rm -f fuelsphereairports.zip && zip -r fuelsphereairports.zip . -x '*.zip'"
  },
  "devDependencies": {
    "@ui5/cli": "^3",
    "@sap/ux-ui5-tooling": "^1"
  }
}
