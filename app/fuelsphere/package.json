{
  "name": "fuelsphere-ui",
  "version": "1.0.0",
  "description": "FuelSphere Fiori Application",
  "private": true,
  "scripts": {
    "start": "fiori run --open \"index.html\"",
    "build": "ui5 build --clean-dest --dest dist",
    "lint": "eslint webapp",
    "deploy": "fiori deploy --config ui5-deploy.yaml",
    "test": "qunit"
  },
  "devDependencies": {
    "@sap/ux-specification": "^1",
    "@ui5/cli": "^4",
    "@ui5/fs": "^4",
    "@sap/ux-ui5-tooling": "^1",
    "eslint": "^8"
  },
  "ui5": {
    "dependencies": [
      "@sap/ux-ui5-tooling"
    ]
  }
}
